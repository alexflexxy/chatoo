from django.apps import AppConfig


class ChatooConfig(AppConfig):
    name = 'chatoo'

#class UserConfig(AppConfig):
 #   default_auto_field = 'django.db.models.BigAutoField'
  #  name = 'users'

   # def ready(self) -> None:
    #    import users.signals
